{
  "$schema": "https://railway.app/railway.schema.json",
  "env": {
    "PYTHON_VERSION": "3.11.8"
  },
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python"],
      "phases": {
        "setup": {
          "nixPkgs": ["...", "nodejs"]
        }
      }
    },
    "buildCommand": "echo 'Running build command' && python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && python --version"
  },
  "deploy": {
    "startCommand": "gunicorn app:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
